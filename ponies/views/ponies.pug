extends layout 

block content
    if ponies.length 
        div.ponies-container 
            each pony in ponies
                div.pony-container  
                    h2  #{pony.name}
                    img(src="/images/" + pony.image)