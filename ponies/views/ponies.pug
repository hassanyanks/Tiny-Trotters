extends layout 

block content
    if ponies.length 
        div.resources-container 
            each pony in ponies
                div.resource-container 
                    h2  #{pony.name}
                    img(src="/images/" + pony.image)
